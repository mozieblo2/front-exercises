export type Routes = {
    [key: string]: string;
};

export const ROUTES: Routes = {
    HOME: '/',
    DRAG_AND_DROP: '/drag-and-drop',
    REDUX: '/redux',
    GENERATOR: '/generator-function',
    GRAPHQL: '/graphql'
};